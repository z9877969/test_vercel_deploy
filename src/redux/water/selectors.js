export const selectWaterData = (state) => state.user.userData.waterData;
export const selectIsLoading = (state) => state.user.userData.isLoading;
export const selectError = (state) => state.user.error;
